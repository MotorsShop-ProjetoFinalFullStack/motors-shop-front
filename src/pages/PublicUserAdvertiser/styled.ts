import styled from "styled-components"

export const PublicUserAdvertiserStyled = styled.body`

    main{
        background-color: var(--gray-8);
        padding: 0 20px;
    }

    .contentPublicPage{
        margin: 0 auto;
        max-width: 1350px;
        display: flex;
        flex-direction: column;
    }

    .contentPublicPage h2{
        font-size: 30px;
        font-weight: 600;
        color: var(--gray-1);
    }

`