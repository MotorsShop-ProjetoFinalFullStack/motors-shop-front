import { styled } from "styled-components";

export const RegisterStyled = styled.div`
  main {
    form {
      margin: 0 auto;
      width: 400px;
      height: 800px;
      background-color: red;
    }
  }
`;
